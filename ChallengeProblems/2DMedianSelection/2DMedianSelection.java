/*
 * Given a 2D array sorted in increasing order on columns
 * and rows, select the median.
 *
 * Approach: 
 *
 * @author Sanjay Paul
 * @date 12.31.2012
 */

public class 2DMedianSelection {

    /*
     * Assumption: All columns are of equal length
     * and all rows are of equal length. This becomes
     * a slightly more difficult problem if that
     * assumption does not hold.
     *
     * @return -1 on failure, otherwise the median
     */
    public static int selectMedianMatrix(int[][] mat) {
        int n = mat.length;
        int m = mat[0].length;
        int 
        if (n<m) {
           for (int i = 0; i <  
        }
        else {

        }
    }
}
